#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "StructTypes.h"

/*Declare the new functions here*/
void PrintMenu(void);
int GetChoice(void);
int Act(int option); //return 0 if the user want to exit ,else return 1
